[package]
name = "hive-schema"
description = "The forward-only migrations and the runner that applies them. Its own crate so a test anywhere can migrate a private schema without a cycle through the store"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
sqlx.workspace = true
thiserror.workspace = true
sha2.workspace = true
hex.workspace = true
